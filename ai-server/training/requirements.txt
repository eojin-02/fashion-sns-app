# 학습 전용 의존성 — 워커(../requirements.txt)와 분리 (DESIGN.md 4절)
# GPU 컴퓨터: https://pytorch.org/get-started/locally/ 에서 CUDA 버전에 맞는
# 설치 명령을 확인해 torch/torchvision을 먼저 설치하는 것을 권장.
torch>=2.4
torchvision>=0.19
pillow>=10.4
onnx>=1.16
onnxruntime>=1.19
